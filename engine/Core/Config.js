
// src/Core/Config.js
const Config = {
    devMode: false, // Controle centralizado
    debugColors: {
        target: 'blue'
    },
    // Você pode adicionar mais configurações aqui (fps, scale, etc)
};

export default Config;